const config = {
    // Set to true to open all days regardless of date (for testing/preview)
    // Set to false to enforce date-based locking
    devMode: true, 

    // Target year for Valentine's week
    year: 2026 
};
